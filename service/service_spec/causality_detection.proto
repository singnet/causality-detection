syntax = "proto3";

message CausalityDetectionRequest {
    string csv = 1;
    string output_column = 2;
    int32 number_of_rows = 3;
}

message PValue {
    string data = 1;
}

service CausalityDetection {
    rpc detect_causality (CausalityDetectionRequest) returns (PValue);
}
